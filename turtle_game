import turtle
my_turtle = turtle.Turtle()
my_turtle.speed(0)
def sq(length):
    for i in range(4):
        my_turtle.forward(length)
        if length < 100:
          my_turtle.left(90)
        else :
          my_turtle.right(90)
    
for i in range(100):
    sq(90)
    my_turtle.left(7)
